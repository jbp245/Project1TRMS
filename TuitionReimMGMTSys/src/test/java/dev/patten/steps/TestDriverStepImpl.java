package dev.patten.steps;

import org.openqa.selenium.WebDriver;

import dev.patten.pages.TestDriver;
import dev.patten.runners.Runner;

public class TestDriverStepImpl {

		public static TestDriver testDriver = Runner.runner;
		public static WebDriver driver = Runner.driver;
		
		
}
